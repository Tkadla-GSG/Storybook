'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.getDateString = getDateString;
exports.getDateTimeString = getDateTimeString;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getDateString(date) {
    return date.getFullYear() + '-' + padZeros(date.getMonth() + 1) + '-' + padZeros(date.getDate());
}

function getDateTimeString(date) {
    return getDateString(date) + 'T' + padZeros(date.getHours()) + ':' + padZeros(date.getMinutes()) + ':' + padZeros(date.getSeconds()) + '.' + padZeros(date.getMilliseconds(), 3);
}

function padZeros(value) {
    var zeros = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 2;

    return _lodash2.default.padStart(value, zeros, '0');
}