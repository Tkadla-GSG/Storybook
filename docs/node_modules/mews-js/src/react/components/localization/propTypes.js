import { PropTypes as pt } from 'prop-types';

export const localizationPropTypes = pt.shape({
    localizeText: pt.func,
    localizeDate: pt.func,
    localizeProperty: pt.func,
    localizeCurrencyValue: pt.func,
});
