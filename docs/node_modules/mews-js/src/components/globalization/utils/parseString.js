import toMoment from './toMoment';

export default function parseString(string, timezone, formats) {
    var m = toMoment(string, formats, timezone);
    if (!m.isValid()) {
        throw new TypeError('The argument is not in a supported format: ' + string);
    }
    return m;
}
