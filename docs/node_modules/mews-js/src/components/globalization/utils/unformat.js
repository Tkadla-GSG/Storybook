import type from '../../../utils/type';
import Localization from '../localizations/localization';
import ensureCultureInfo from './ensureCultureInfo';
import toMoment from './toMoment';

export default function unformat(value, localization, formatStringName, cultureInfoComponent, timezone) {
    type.check(localization, Localization);

    const cultureInfo = ensureCultureInfo(localization.getCulture(), cultureInfoComponent);
    return toMoment(value, cultureInfo.getDateTimeInfo()[formatStringName], timezone);
}
