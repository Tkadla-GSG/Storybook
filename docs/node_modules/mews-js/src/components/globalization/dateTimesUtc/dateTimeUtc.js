import { type } from '../../../utils';
import MomentFormat from '../enums/momentFormat';

export default function DateTimeUtc(moment) {
    return type.create(DateTimeUtc, {
        value: moment.format(MomentFormat.DateTimeUtc.value),
    });
}
