import { type } from '../../../utils';

export default function Country(data) {
    return type.create(Country, {
        getCode() {
            return data.code;
        },

        getName() {
            return data.name;
        },
    });
}
