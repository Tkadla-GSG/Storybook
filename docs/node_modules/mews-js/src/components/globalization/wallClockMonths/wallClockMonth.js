import { type } from '../../../utils';
import MomentFormat from '../enums/momentFormat';

export default function WallClockMonth(moment) {
    return type.create(WallClockMonth, {
        value: moment.format(MomentFormat.Month.value),
    });
}
