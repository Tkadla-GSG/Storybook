import { type } from '../../../utils';

export default function Localization(language, culture) {
    return type.create(Localization, {
        getLanguage() {
            return language;
        },

        getCulture() {
            return culture;
        },
    });
}
