import { type } from '../../../utils';
import MomentFormat from '../enums/momentFormat';

export default function WallClockDate(moment) {
    return type.create(WallClockDate, {
        value: moment.format(MomentFormat.Date.value),
    });
}
