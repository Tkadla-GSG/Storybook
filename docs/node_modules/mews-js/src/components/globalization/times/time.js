import { type } from '../../../utils';
import IsoFormat from '../enums/isoFormat';

export default function Time(moment) {
    return type.create(Time, {
        value: moment.format(IsoFormat.TimeMilliseconds.value),
    });
}
