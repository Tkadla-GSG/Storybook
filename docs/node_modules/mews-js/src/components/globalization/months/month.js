import { type } from '../../../utils';
import IsoFormat from '../enums/isoFormat';

export default function Month(moment) {
    return type.create(Month, {
        value: moment.format(IsoFormat.Month.value),
        timezone: moment.tz(),
    });
}
