import { type } from '../../../utils';
import IsoFormat from '../enums/isoFormat';

export default function DateTime(moment) {
    return type.create(DateTime, {
        value: moment.format(IsoFormat.Milliseconds.value),
        timezone: moment.tz(),
    });
}
