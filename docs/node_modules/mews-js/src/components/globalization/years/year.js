import { type } from '../../../utils';
import MomentFormat from '../enums/momentFormat';

export default function Year(moment) {
    return type.create(Year, {
        value: moment.format(MomentFormat.Year.value),
        timezone: moment.tz(),
    });
}
