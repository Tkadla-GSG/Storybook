import { type } from '../../../utils';

export default function Culture(data) {
    return type.create(Culture, {
        getCode() {
            return data.code;
        },

        getEnglishName() {
            return data.englishName;
        },

        getLocalName() {
            return data.localName;
        },
    });
}
