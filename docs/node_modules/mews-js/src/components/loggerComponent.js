import { type } from '../utils';

export default function LoggerComponent(configurationComponent) {
    return type.create(LoggerComponent, {
        log(...args) {
            if (!configurationComponent.isProduction() && global.console) {
                global.console.log(...args);
            }
        },
    });
}
