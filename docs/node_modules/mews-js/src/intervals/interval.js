import type from '../utils/type';
import _ from 'lodash';

export default function Interval({
    high = null,
    low = null,
} = {}) {
    const start = _.min([high, low]);
    const end = _.max([high, low]);
    return type.create(Interval, { high: end, low: start });
}
