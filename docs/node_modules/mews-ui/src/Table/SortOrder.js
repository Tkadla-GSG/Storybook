import { enumify } from 'mews-js';

export default function SortOrder() {}
enumify(SortOrder, {
    Ascending: {
        value: 'asc',
        getOpposite: () => SortOrder.Descending,
    },
    Descending: {
        value: 'desc',
        getOpposite: () => SortOrder.Ascending,
    },
});
