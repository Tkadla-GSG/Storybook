import { enumify } from 'mews-js';

export default function ColumnAlignment() {}
enumify(ColumnAlignment, {
    Left: {
        value: 'left;',
    },
    Center: {
        value: 'center;',
    },
    Right: {
        value: 'right;',
    },
});
