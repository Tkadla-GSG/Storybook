import styled from 'styled-components';
import { FONT_SIZE_LARGE, FONT_SIZE_NORMAL } from '../variables';
import IconButton from '../Button/IconButton';
import { OpenableOverlayElement } from '../OpenableOverlay/style';

export const TimePickerContainerElement = styled.div`
    position: relative;

    ${OpenableOverlayElement} {
        margin-top: 16px;
    }
`;

export const TimePickerInputElement = styled.div`
    align-items: center;
    cursor: pointer;
    display: flex;
    font-size: ${props => (props.large ? FONT_SIZE_LARGE : FONT_SIZE_NORMAL)};
    min-height: ${props => (props.large ? '30px' : '20px')};
    outline: none;
    position: relative;
`;

export const TimePickerButtonElement = styled(IconButton)`
    position: absolute;
    right: -19px;
`;

TimePickerButtonElement.defaultProps = {
    icon: 'arrow_drop_down',
};
