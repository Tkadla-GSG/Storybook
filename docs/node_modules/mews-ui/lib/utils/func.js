export function noop() {
    // empty
}

export function debounce(func) {
    var wait = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

    var handle = void 0;

    var debounced = function debounced() {
        if (handle) {
            clearTimeout(handle);
        }

        handle = setTimeout(func, wait);
    };

    debounced.cancel = function () {
        clearTimeout(handle);
    };

    return debounced;
}