import { enumify } from 'mews-js';

export default function Position() {}

enumify(Position, {
    Top: 'top',
    Bottom: 'bottom',
    Left: 'left',
    Right: 'right',
    Horizontal: 'horizontal',
    Vertical: 'vertical'
});